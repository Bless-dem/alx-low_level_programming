#include "main.h"

/**
 * main - a program that prints the name, followed by a new line
 * @argc: counts the total number of arguments
 * @argv: the array of each number of arguments passed to the program
 *
 * Return: 0 (success)
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
